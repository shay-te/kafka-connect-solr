<?xml version="1.0" encoding="UTF-8" ?>
<config>
  <luceneMatchVersion>9.8.0</luceneMatchVersion>
  <dataDir>${solr.data.dir:}</dataDir>
  <directoryFactory name="DirectoryFactory"
                    class="${solr.directoryFactory:solr.NRTCachingDirectoryFactory}"/>
  <codecFactory class="solr.SchemaCodecFactory"/>
  <schemaFactory class="ClassicIndexSchemaFactory"/>

  <updateHandler class="solr.DirectUpdateHandler2">
    <updateLog>
      <str name="dir">${solr.ulog.dir:}</str>
    </updateLog>
    <autoCommit>
      <maxTime>${solr.autoCommit.maxTime:15000}</maxTime>
      <openSearcher>false</openSearcher>
    </autoCommit>
  </updateHandler>

  <!-- Default update chain: ignore any write whose _offset_ver is <= the stored one.
       This is what stops an out-of-order (older Kafka offset) concurrent write from
       clobbering a newer version of the same id. -->
  <updateRequestProcessorChain name="offset-versioning" default="true">
    <processor class="solr.DocBasedVersionConstraintsProcessorFactory">
      <str name="versionField">_offset_ver</str>
      <bool name="ignoreOldUpdates">true</bool>
    </processor>
    <processor class="solr.LogUpdateProcessorFactory"/>
    <processor class="solr.RunUpdateProcessorFactory"/>
  </updateRequestProcessorChain>

  <requestHandler name="/update" class="solr.UpdateRequestHandler"/>
  <requestHandler name="/select" class="solr.SearchHandler">
    <lst name="defaults">
      <str name="echoParams">explicit</str>
      <str name="df">id</str>
    </lst>
  </requestHandler>

  <query>
    <maxBooleanClauses>1024</maxBooleanClauses>
    <filterCache class="solr.CaffeineCache" size="128" initialSize="0" autowarmCount="0"/>
    <queryResultCache class="solr.CaffeineCache" size="128" initialSize="0" autowarmCount="0"/>
    <documentCache class="solr.CaffeineCache" size="128" initialSize="0" autowarmCount="0"/>
  </query>
</config>
